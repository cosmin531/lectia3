let numar = 5
if (numar % 2 == 0) {
    basic.showIcon(IconNames.No)
    basic.showString("Par")
} else {
    basic.showIcon(IconNames.Yes)
    basic.showString("Impar")
}

numar = 2
if (numar % 2 == 0) {
    basic.showIcon(IconNames.Yes)
    basic.showString("Par")
} else {
    basic.showIcon(IconNames.No)
    basic.showString("Impar")
}

